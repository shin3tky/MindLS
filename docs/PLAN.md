# Mind Language Server 開発計画

対象: 日本語プログラミング言語 **Mind**（Scripts Lab Inc.）
　　　言語仕様は Version 9 を基準。検証環境は Linux 版が最新の **Version 8**（後述 2.5）
成果物: VS Code 向け拡張 + LSP 準拠 Language Server
実装スタック: **TypeScript 一本**（`vscode-languageserver-node`）
最終更新: 2026-09-16（Mind-Docker への分離、M0 / M1 完了、標準単語辞書の生成を反映）

> **更新履歴**
> - 2026-09-14 初版
> - 2026-09-14 Docker 検証環境の構築完了を反映。
> - 2026-09-16 処理系の Docker 環境を [Mind-Docker](https://github.com/shin3tky/Mind-Docker) に分離。
>   npm workspaces のスケルトンを作成。**M0（正規化エンジン）完了**、
>   **標準単語辞書 `stdlib.json` を生成**（file ライブラリ 692 語 global / 266 語 local）。
> - 2026-09-16 **M1（拡張スケルトン + ハイライト）完了**。tmLanguage をトークン化テストで固定（104 件 green）。
>   「確認したい事項」のうち 2 件が解決し、M0 の差分テストと M4 の辞書自動生成が実行可能になった

---

## 1. ゴールとスコープ

### 目的

Mind のソースコードを VS Code で書くときに、他の現代的な言語と同等の編集体験を得る。

### MVP（今回の到達点）

| # | 機能 | LSP メソッド |
|---|------|-------------|
| 1 | 構文ハイライト | TextMate 文法（+ 将来 Semantic Tokens） |
| 2 | アウトライン / 単語一覧 | `textDocument/documentSymbol`, `workspace/symbol` |
| 3 | 定義ジャンプ | `textDocument/definition` |
| 4 | 参照検索 | `textDocument/references` |
| 5 | 補完 | `textDocument/completion` |
| 6 | ホバー | `textDocument/hover` |

### MVP 後（同一アーキテクチャ上で追加）

診断（自前解析 + 実コンパイラ連携）、リネーム、フォーマッタ、Semantic Tokens、コードアクション。

### 非スコープ（当面）

- GUI 編（`samplew`）専用の解析
- 構造体・型紙の完全な型検査
- デバッガ（DAP）

---

## 2. 設計の前提 — Mind が普通の言語と決定的に違う点

ここが本プロジェクトの技術的な核心であり、設計判断の大半がここから導かれる。

### 2.1 単語は「正規化」してから照合される

Mind のコンパイラは、ソース上の文字列をそのまま識別子として扱わない。以下の変換を行ってから
シンボルテーブルを引く。

| 規則 | 例 |
|------|-----|
| 送り仮名（漢字に続くひらがな）を削除 | `反応し` = `反応する` = `反応させる` → `反応` |
| 単語内のひらがなも削除 | `赤い色で表示する` → `赤色表示` |
| ただし先頭のひらがなは保持 | `ご案内する` → `ご案内`（≠ `案内`） |
| ひらがなのみの単語は末尾の助詞だけ削除 | `つぎに` → `つぎ` 相当の扱い |
| 中点を無視 | `トータル・カウント` = `トータルカウント` |
| 半角カナ ≡ 全角カナ（濁点は合成） | `ｶﾞ` → `ガ` |
| 半角英数記号 ≡ 全角英数記号、大文字 ≡ 小文字 | `VIDEO/8` = `video/8` = `ＶＩＤＥＯ／８` = `ｖｉｄｅｏ／８` |
| 数字に続く非数字（助数詞）は無視 | `５６０円` → `560`、`１０００円` → `1000` |
| 否定形の送り仮名はエラー | `反応させない` → コンパイルエラー |

**帰結**: ハイライトも補完も定義ジャンプも、この正規化関数 `normalize()` が正しくなければ
すべて外れる。**これを最初に作り、テストで固める。**

### 2.2 単語の区切りは「分かち書き」

区切りに使えるのは次のみ。**それ以外はすべて単語の一部**。

```
TAB / 空白（半角・全角）/ カンマ（半角 , 全角 ，）/ 読点（、）
```

したがって `赤い色で表示する` は 1 単語、`赤い色で 表示する` は 2 単語であり、
両者の間に類似性は一切ない。**改行も区切りとして働く**（ただし `または` / `かつ` は行が意味を持つ）。

### 2.3 単語は使用前に定義されていなければならない

下位の部品から順に定義する。前方参照は `仮定義` / `本定義` で明示的に行う。

```
二乗とは       仮定義。
二乗とは 本定義
	複写し、掛けること。
```

**帰結**: 単一パスのトップダウン解析でスコープが確定する。これは LSP 実装にとって非常に都合が良い。
「定義より前で参照されている」は **確実に検出できる診断**になる。

### 2.4 トップレベルの `終り。` 以降はコンパイルされない

```
終り。          ← ここでコンパイル終了
◎◎とは        ← 以降は解析対象外
```

解析器はここで打ち切り、以降の領域は「コンパイルされない領域」としてグレーアウト表示できる（付加価値）。

### 2.5 プラットフォームと文字コード（配布物を実際に開いて確認済み）

**Mind は OS ごとにバージョンが分かれている。これは検証計画に直接効く。**

| | Windows | Linux |
|---|---|---|
| 最新版 | **Version 9**（2026/08/30, 9.04） | **Version 8**（`mind-for-linux-8.0.08`, 2021/08/14） |
| 文字コード | **Shift_JIS** | **EUC-JP** |
| バイナリ | — | **x86 32bit**（ELF 32-bit LSB / Intel 80386） |
| GUI 編（`guilib`） | あり | なし |

**Mind 9 に Linux 版は存在しない。** よって手元（macOS / Apple Silicon）で動かせる実処理系は
**Mind 8 for Linux** であり、Docker の platform は `linux/amd64` ではなく **`linux/amd64`** になる。
Apple Silicon の Rosetta は 32bit x86 を扱えないため QEMU エミュレーションとなる（→ 遅い。M5 の設計に影響）。

送り仮名の削除・正規化・分かち書き・制御構文といった**言語のコアは 8 と 9 でほぼ共通**なので、
M0 の答え合わせには Mind 8 で十分。Mind 9 固有の追加単語と GUI 編だけが未検証領域として残る。

#### ファイル形式

| 項目 | 値 |
|------|-----|
| ソース拡張子 | **`.src`**（Mind WorkBench では新規作成時に強制される） |
| 実行ファイル | `.exe`（Linux は拡張子なし）+ `.mco`（M コード＝実体） |
| 辞書 | `.sym`（**バイナリ**。テキストではないので直接は読めない） |
| 履歴 | `.his`（**テキスト**。ソース名・更新日時・**リンク先ライブラリ名**・起動引数） |
| エラー出力 | `.inf`（インフォメーションファイル。**文法エラー時のみ生成**） |

VS Code の既定は UTF-8 なので対策が要る:
- ワークスペース設定テンプレート（`"[mind]": { "files.encoding": "shiftjis" }` / Linux 運用なら `eucjp`）を
  生成するコマンドを拡張に持たせる
- Language Server は VS Code から UTF-8 で受け取るので内部処理は UTF-8 のまま
- **実コンパイラ連携時のみ**、一時ファイルへ Shift_JIS/EUC-JP で書き出し、
  返ってきたバイトオフセットを UTF-16 コードユニット位置へ逆変換する（後述 M5）
- 検証環境ではこの変換を `docker/bin/mindc` ラッパーが担当済み（`.mindbuild/` に EUC-JP の影ソースを作る）。
  **LS 側のオフセット変換テーブルは、この影ソースと元の UTF-8 ソースを突き合わせれば確実に作れる**

### 2.6 コンパイル単位は「ライブラリ + ソース」

Mind のコンパイルは下位ライブラリ（`lib/file`, `lib/guilib` など）に**直列にリンク**して行われる。
どのライブラリにリンクするかで可視な標準単語が変わるため、LS も同じ情報を持つ必要がある。

- 設定 `mind.library`（既定 `file`、GUI なら `guilib`）
- **`.his` がテキストなので、既にコンパイル済みのソースはここからリンク先ライブラリを自動判定できる**
- 1 プログラムが複数ソースに分かれる（副ソースの読み出し）ケースがあるため、
  **マルチファイルのシンボル解決**を最初から設計に入れる

### 2.7 実処理系の検証環境 — **別リポジトリに分離済み** ✅

処理系の Docker 環境は [Mind-Docker](https://github.com/shin3tky/Mind-Docker) が持つ。
MindLS はそのイメージと CLI 契約だけに依存する。詳細は **`docs/COMPILER-BACKEND.md`**。

```sh
cd ../Mind-Docker && make build && make selftest   # mind-docker:8.0.08 ができる
```

UTF-8 のソース → EUC-JP → 本物の `mind` → 実行ファイル + `.mco` + `.sym` + `.his` まで、
Windows 11 + WSL2 / macOS (Apple Silicon) / x86_64 Linux で**通ることを確認済み**。

これにより次が可能になった。

| できるようになったこと | どこで使うか | Docker |
|---|---|---|
| 標準ライブラリ `.src` からの**辞書自動生成** | M4（**完了**。配布物から直接読む） | **不要** |
| 正規化規則・分かち書き・構文の**答え合わせ** | M0 の差分テスト | 必要（任意実行） |
| `.inf` の実物を得てフォーマットを確定 | M5 のパーサ実装 | 必要（オプトイン） |

**制約**: Apple Silicon ではエミュレーションになるぶん遅い。
**Language Server 本体はホスト側ネイティブの Node で動かし、コンテナは「保存時に叩くバックエンド」**
として使う。打鍵ごとの補完・ホバーは自前解析だけで完結させる。

**拡張の利用者に Docker を要求しない。** Docker から取り出したものは生成物としてコミットする。

---

## 3. アーキテクチャ

```
┌──────────────────────────────────────────────┐
│ vscode-mind（VS Code 拡張 / クライアント）                    │
│  ・言語登録（.src / .mnd）・TextMate 文法                     │
│  ・language-configuration.json（コメント・括弧・インデント）    │
│  ・LanguageClient 起動・設定 UI                              │
└───────────────┬──────────────────────────────┘
                │ LSP (stdio)
┌───────────────▼──────────────────────────────┐
│ mind-language-server                                          │
│  ・TextDocuments 管理 / 差分更新                              │
│  ・各 LSP ハンドラ（symbol / definition / completion / ...）   │
│  ・診断アグリゲータ（自前解析 ＋ 外部コンパイラ）               │
└───────────────┬──────────────────────────────┘
                │
┌───────────────▼──────────────────────────────┐
│ mind-core（エディタ非依存・純 TypeScript）                     │
│                                                               │
│  normalizer.ts   文字幅/大小/中点/送り仮名の正規化             │
│  lexer.ts        分かち書き → Token[]                         │
│  parser.ts       定義・宣言・制御構文の構造化                   │
│  symbols.ts      シンボルテーブル（大域 / 局所 / 局所処理単語）  │
│  stdlib.json     標準単語辞書（名前・種別・スタック仕様・説明）  │
└──────────────────────────────────────────────┘
```

**`mind-core` をエディタから完全に切り離す**のが要点。単体テストが書きやすくなり、
将来 CLI リンタや別エディタ対応に流用できる。

### トークンの形

正規化で情報を捨ててはいけない。トークンは生テキストと正規形の両方を持つ。

```ts
interface MindToken {
  raw: string;            // "売り上げ計上とは"
  normalized: string;     // "売上計上"        ← 照合用キー
  trailingParticle?: string; // "とは"          ← 構文判定用
  kind: TokenKind;        // Word | Number | String | Char | Comment | ...
  range: Range;           // UTF-16 ベース
}
```

- **構文判定は `raw` の末尾（`とは` / `は` / `を` / `に` / `へ` / `と` / `から`）で行う**
- **シンボル照合は `normalized` で行う**

`とは`／`は` を送り仮名として消してしまうと定義が検出できなくなるため、
正規化の前に助詞を切り出して保持する。

---

## 4. 押さえるべき Mind の構文一覧

実装時のチェックリストとして使う。

### 4.1 定義・宣言

| 構文 | 例 |
|------|-----|
| 処理単語 | `○○とは … 。`（`とは` は `は` でも可） |
| 処理単語（属性付き） | `○○とは 処理単語 整数入力 整数入力`（改行不可） |
| 関数 | `○○とは 関数 整数入力 整数出力`（改行不可、引数 1〜3） |
| 仮定義 / 本定義 | `○○とは 仮定義。` → `○○とは 本定義 …。` |
| 整数変数 | `○○は 変数。` |
| 小数変数 | `○○は 小数変数。` |
| ワード / バイト変数 | `○○は ワード変数。` / `○○は バイト変数。` |
| 文字列情報変数 | `○○は 文字列。` |
| 文字列実体変数 | `○○は 文字列実体 長さ １００桁。` |
| 数値定数 | `○○は 定数 ２５０度。` / `○○は 定数 <Mind式>。` |
| 文字列定数 | `○○は 文字列定数 "dir /w"。` |
| 等価定義 | `○○は △△と 等価。` |
| 等価な関数 | `○○は △△と 等価な関数[2|3]。` |
| 局所変数 | 定義の冒頭に `△△は 変数`（**末尾に `。` を付けない**） |
| 局所処理単語 | 定義内にネスト。末尾 `。` なし。本体開始に `本体とは` ダミー宣言 |
| エントリポイント | `メインとは …。` |

**診断ネタ**: 局所変数に `。` を付けると処理単語の定義終了と誤認される — これは実際に起きやすいミスなので警告対象。

### 4.2 制御構文キーワード

```
もし（擬似語・無意味）  ならば  さもなければ  でなければ  つぎに
ここから  繰り返す  打ち切り  もう一度
回数指定  回数
終り  実行終り
選択する … 選択終り
事例をとる / 文字列事例をとる / 範囲事例をとる … 例外なら / 範囲外なら … 事例終り
無処理
または  かつ        ← 例外的に中置記法。影響範囲は「行」
コンパイル抑止。 … コンパイル抑止終り。
```

**対応関係の検査**（診断の主力）:
- `ならば` / `でなければ` ↔ `つぎに`
- `ここから` / `回数指定` ↔ `繰り返す`
- `選択する` ↔ `選択終り`
- `事例をとる` ↔ `事例終り`（`例外なら` は必須）
- 定義の `。` 閉じ忘れ

### 4.3 リテラル

| 種別 | 記法 |
|------|------|
| 整数 | `-123` / `－１２３`（全角可） |
| 小数 | `3.14` / `1.23E2` / `-1.23E-2`（仮数 15 桁、指数 ±308） |
| 8 進 | `8x644`（基数部は 2〜16 指定可） |
| 16 進 | `0xaf64de89` / `6800h` / `0af64de89H` |
| 文字 | `'A'` / `'漢'`（シングルクォート半角・全角可） |
| 文字列 | `「…」` / `"…"`（開閉の半/全角は統一必須、最大 158 文字） |
| 文字列の行継続 | 末尾に `続` |
| 文字列内制御コード | `＆改行＆` `＆ＣＲＬＦ＆` `＆ＴＡＢ＆` `＆ＥＳＣ＆` `＆0D0A＆` |

### 4.4 コメント

| 記法 | 注意 |
|------|------|
| `（ … ）` | **両端に空白が必要**。空白がなければ配列添字・演算優先の括弧 |
| `※` 〜 行末 | |
| `コンパイル抑止。` 〜 `コンパイル抑止終り。` | ブロックコメント相当 |

**スタック仕様コメント** `（金額 → ・）` は定義行に書かれる慣習で、
**ホバーとシグネチャヘルプの情報源として最重要**。これを構造化して取り込む。

```
売り上げ計上とは　（単価、個数　→　・）
```
→ ホバー表示: `売り上げ計上 (処理単語)  単価, 個数 → ・`

---

## 5. リポジトリ構成

`✅` は実装済み、`⬜` はこれから。

```
MindLS/
├─ ✅ package.json                # npm workspaces（Node 22 / ESM）
├─ ✅ tsconfig.base.json  tsconfig.json  vitest.config.ts
├─ ✅ .gitattributes              # 改行は LF 固定（Windows 対策）
├─ packages/
│  ├─ mind-core/                  # エディタ非依存のコア
│  │  ├─ ✅ src/normalizer.ts     # 正規化・助詞の切り出し・否定形の検出
│  │  ├─ ⬜ src/{lexer,parser,symbols}.ts
│  │  ├─ ✅ data/stdlib.json      # 標準単語辞書（生成物だがコミットする）
│  │  └─ ✅ test/                 # Vitest ゴールデンテスト
│  ├─ mind-compiler/              # 実処理系アダプタ
│  │  └─ ⬜ src/{adapter,docker,local,inf-parser,offset-map}.ts
│  ├─ mind-language-server/
│  │  └─ ⬜ src/{server,handlers/,diagnostics/}.ts
│  └─ vscode-mind/
│     ├─ ✅ package.json           # contributes.languages / grammars / configuration / commands
│     ├─ ✅ language-configuration.json
│     ├─ ✅ syntaxes/mind.tmLanguage.json
│     ├─ ✅ src/extension.ts       # 文字コードと関連付けを設定するコマンド
│     └─ ✅ test/                  # vscode-textmate でのトークン化テスト
├─ tools/
│  ├─ ✅ gen-stdlib-dict.ts       # 配布物から標準単語辞書を生成（Docker 不要）
│  ├─ ✅ extract-samples.ts       # 公式サンプルを UTF-8 で展開（Git 管理外）
│  └─ ⬜ diff-normalizer.ts       # 実コンパイラとの差分テストドライバ
├─ fixtures/
│  ├─ ✅ hello.src  syntax.src    # 文法テスト用（コミットする）
│  └─ ⬜ mind-samples/            # 公式サンプルの展開先（Git 管理外）
└─ docs/
   ├─ ✅ PLAN.md                  # 本書
   └─ ✅ COMPILER-BACKEND.md      # 実 Mind コンパイラとの結合（DOCKER.md を置き換え）
```

処理系の Docker 環境は **[Mind-Docker](https://github.com/shin3tky/Mind-Docker)（別リポジトリ）**。
MindLS には取り込まない。二重メンテを避けるため、結合点はイメージ名と CLI 契約の 1 点だけにする。

---

## 6. マイルストーン

### M-1 — Mind 処理系の Docker 環境 — **完了** ✅

別リポジトリ [Mind-Docker](https://github.com/shin3tky/Mind-Docker) として分離。
3 プラットフォームで `make selftest` 通過。詳細は `docs/COMPILER-BACKEND.md`。

### M0 — 正規化エンジンの PoC（1〜2 日）

**これを最初にやる。** ここが崩れると全部崩れる。

- `normalize(word: string): string` を実装
- 公式マニュアルに出てくる等価例をすべてゴールデンテスト化
  （`反応し`/`反応する`/`反応させる`、`赤い色で表示する`→`赤色表示`、
  `ご案内`≠`案内`、`VIDEO/8` 4 パターン、`トータル・カウント`、助数詞、否定形エラー）
- **NFKC を素で使わない**。`〜`/`ー`/`～` など意図しない変換があるため自前テーブルで幅・大小を寄せる
- **差分テスト（M-1 で可能になった本命）**: `tools/diff-normalizer.ts` で
  「表記の異なる 2 つの単語が同一とみなされるか」を生成し、実コンパイラに判定させる。
  具体的には `○○とは …。` を定義した直後に表記違いで参照する `.src` を機械生成し、
  コンパイルが通れば同値・`.inf` が出れば非同値、という形で正規化規則を総当たり検証する
  （QEMU で遅いので CI / 夜間バッチ向き。単体テストのループには入れない）
- 完了条件: 全ゴールデンテストがグリーン + 差分テストで既知の不一致がゼロ

### M1 — 拡張スケルトン + ハイライト — **完了** ✅

`packages/vscode-mind/` として実装。

- `.src` / `.mnd` を言語 `mind` に登録。`.src` は他言語とも衝突するため、
  コマンド **`Mind: このワークスペースの文字コードと関連付けを設定する`** で
  ワークスペースに `files.associations` と `[mind].files.encoding` を書き込めるようにした
- `language-configuration.json`
  - コメント: 行 `※`、ブロック `（ ␣ … ␣ ）`（**両端の空白込みで登録**。空白が無いと配列添字になるため）
  - 括弧ペア: `「」` `『』` `（）` `()`
  - `wordPattern` を分かち書きに合わせた（既定の単語判定では日本語の単語が切れる）
  - `とは` / `は` / `ならば` / `ここから` の後で字下げ、`。` で戻す
- `syntaxes/mind.tmLanguage.json`
  - コメント・文字列・文字定数・数値・制御構文・宣言語・定義行の単語名・助詞
  - 制御構文は**送り仮名の表記ゆれを列挙**している。取りこぼしより取りすぎのほうが害が大きいため、
    厳密な区別は M6 の Semantic Tokens に回す

**検証は目視ではなくトークン化テストでおこなう。** VS Code と同じエンジン
（`vscode-textmate` + `vscode-oniguruma`）でフィクスチャをトークン化し、スコープを固定した。

| テスト | 内容 |
|---|---|
| `grammar.test.ts`（55 件） | コメント・リテラル・定義・宣言・制御構文・助詞のスコープを 1 つずつ固定 |
| `grammar-samples.test.ts`（15 件） | 公式サンプル全 15 本。暴走トークン無し・文字列/コメントが飲み込んでいない・定義が取れている |

とくに効いた検証:

- `（ここから初期化）` は**コメント**、`配列（１）` は**配列添字**。両端の空白で判定できている
- `一行表示すること。` の `と` を助詞と誤判定しない（直前がひらがなの場合は送り仮名とみなす）
- `表示用エンコード０` の `０` を数値として着色しない

公式サンプルは再配布しないため Git には入れていない。
`node tools/extract-samples.ts` で展開したときだけサンプルのテストが走る。

### M2 — レキサ / パーサ / シンボルテーブル（3〜5 日）

- `lexer.ts`: 分かち書き + リテラル + コメント + `終り。` 打ち切り
- `parser.ts`: 定義・宣言・制御構文ブロックの構造化（4.1 / 4.2 全対応）
- `symbols.ts`:
  - 大域シンボル（処理単語 / 関数 / 変数 / 定数 / 等価語）
  - 局所変数（定義の冒頭のみ、定義終端で破棄）
  - 局所処理単語（親からのみ可視）
  - **`normalized` をキー、複数の `raw` を別名として保持**
- 完了条件: `fixtures/` 全ファイルをパースでき、シンボル数が期待値と一致
  - `fixtures/` には配布物同梱の `pmind/sample/`（6 本）`sampleF/`（9 本）`file/`（46 本）を
    UTF-8 に変換して取り込む。**46 本の実ライブラリソースが通れば実用強度**

### M3 — Document Symbol / 定義ジャンプ / 参照（2〜3 日）

- `documentSymbol`: 処理単語をトップ、局所変数・局所処理単語を子に
- `workspace/symbol`: 正規化キーでの横断検索（`ひょうじ` でも `表示` がヒットする）
- `definition` / `references`: 正規化キーで照合
- 完了条件: sample 上でジャンプが正しく動く

### M4 — 補完 / ホバー（3〜4 日）

- 補完候補
  1. その位置で可視なユーザー定義単語
  2. 標準単語辞書（`stdlib.json`）
  3. 制御構文キーワード
  4. スニペット（`ならば…つぎに`、`ここから…繰り返す`、`事例をとる…事例終り`）
- **フィルタ**: VS Code の既定フィルタは日本語の部分一致に弱いため、
  `filterText` に正規形＋読みを入れる。`sortText` で可視スコープを優先
- ホバー: 種別 + スタック仕様コメント + 定義位置の抜粋
- 標準単語辞書の作り方 — **A 案で確定**（配布物を開いて確認済み）
  - `.sym` はバイナリなので直接は使えないが、**標準ライブラリの `.src` が配布物に同梱されていた**

    | 場所 | `.src` 数 | 中身 |
    |---|---|---|
    | `pmind/file/` | **46** | 標準ライブラリ `file` 本体（`file.src` `fopen.src` `console.src` `coutput.src` …） |
    | `pmind/tool/` | 15 | 付属ツール |
    | `pmind/cgilib/` | 11 (+sample 8) | CGI ライブラリ |
    | `pmind/socketlib/` | 2 (+sample 7) | ソケットライブラリ |
    | `pmind/sample/` `sampleF/` | 6 / 9 | サンプル（`fixtures/` 取り込み候補） |

  - `tools/gen-stdlib-dict.ts` が `pmind/file/*.src` の定義行とスタック仕様コメントを
    パースして `stdlib.json` を生成する。**当初の「B（保険）案」は不要になった**
  - 取り出し方: `npm run gen:stdlib   # 配布物の tgz から直接読む。Docker は不要`
  - 規模感: 標準ライブラリ `file` で約 800 単語（`mhist` の total words より）
  - **注意**: これは Mind **8** の辞書。Mind 9 で追加された単語と GUI 編（`guilib`）は含まれない。
    Windows 環境が用意できた時点で差分を取り込む
- 完了条件: `表` と打って `表示` `一行表示` `数値表示` が候補に出る

### M5 — 診断（4〜6 日）

**採用方針: 自前解析を主、実コンパイラ連携を従（オプトイン）**

#### 6.1 自前解析による診断（常時・リアルタイム）

| 検出 | 重大度 |
|------|--------|
| 未定義単語の参照 | Error |
| 定義より前での参照（`仮定義` なし） | Error |
| 定義の `。` 閉じ忘れ | Error |
| `ならば` に対応する `つぎに` がない等の構文対応ミス | Error |
| `事例をとる` に `例外なら` がない | Error |
| 局所変数宣言に `。` を付けている | Warning |
| 否定形の送り仮名（`〜させない`） | Error |
| 文字列定数が 158 文字超（`続` なし） | Warning |
| トップレベル `終り。` 以降のコード | Hint（グレーアウト） |
| `（ ）` コメントの両端に空白がない | Warning |

#### 6.2 実コンパイラ連携（オプトイン・保存時）— **バックエンドは構築済み**

設定 `mind.compiler.docker`（または `mind.compiler.path`）が設定されているときのみ有効。
呼び出し先は **素の `mind` コマンド**（`mindc` ラッパーは使わない）。
文字コード変換とオフセットの逆変換は `@mindls/compiler` が持つ。理由は `docs/COMPILER-BACKEND.md` 4 章。

```
保存
 └→ docker exec <常駐コンテナ> mind <base> file   # 一時ディレクトリの中で実行
      └→ mindc が .mindbuild/ に EUC-JP の影ソースを生成
      └→ 本物の mind がコンパイル（前回の .inf は mindc が削除済み）
      └→ 出力と .inf を EUC-JP → UTF-8 に戻して元の場所へ書き戻す
 └→ .inf を読み取り、即座にクローズ
 └→ EUC-JP バイトオフセット → 行・UTF-16 列 に逆変換して Diagnostic 化
```

**実装上の注意**:
- `.inf` は**文法エラーがあったときのみ生成**される。前回の `.inf` が残っていると誤検出するので、
  実行前に必ず削除する（`mindc` は実装済み）
- **`.inf` を開いたまま再コンパイルするとアクセス競合でコンパイルが失敗する**（公式に明記）。
  LS は読み終わったら必ずハンドルを解放する
- `.inf` 内の位置情報は **EUC-JP のバイトオフセット**。逆変換テーブルは
  `.mindbuild/<name>.src`（EUC-JP の実体）と元の UTF-8 ソースを突き合わせて作るのが確実
- 生成物（実行ファイル / `.mco` / `.sym` / `.his`）でワークスペースを汚さないよう、
  LS から叩くときは `MIND_BUILD_DIR` を一時ディレクトリに向ける
- **`docker compose run` は毎回コンテナを起動する。QEMU 経由なので起動コストが無視できない。**
  保存頻度が上がるなら常駐コンテナへ `docker exec` する方式に切り替える

**環境の選択肢**（現状の整理）:

| 手段 | 状態 | 用途 |
|---|---|---|
| **Docker `linux/amd64` + Mind 8 for Linux** | ✅ 構築済み | 既定。診断と M0 の差分テスト |
| Windows マシン / VM に Mind 9 | 未着手 | Mind 9 固有単語・GUI 編の検証 |
| コンパイラ連携なし（自前診断のみ） | いつでも可 | macOS 単体・CI 軽量パス |

### M6 — 仕上げ（3〜5 日）

- Semantic Tokens（tmLanguage では原理的に無理な「ユーザー定義単語 vs 標準単語」の区別）
- リネーム（`prepareRename` + 全参照置換。送り仮名の違いを保ったまま置換する必要あり）
- フォーマッタ（`とは` 改行、定義内容の字下げ、`ならば`/`つぎに` の整列 — 慣習に沿った整形）
- README / CHANGELOG / アイコン、`vsce package` → Marketplace 公開

---

## 7. テスト戦略

| レイヤ | 手法 |
|--------|------|
| 正規化 | ゴールデンテスト（マニュアルの等価例を網羅）＋ 実コンパイラとの差分テスト |
| レキサ / パーサ | `fixtures/*.src` → スナップショット |
| シンボル | 期待シンボル表との突き合わせ |
| LSP ハンドラ | `vscode-languageserver` をインメモリ接続して結合テスト |
| 拡張 | `@vscode/test-electron` で E2E（最小限） |

**2 層に分ける。**

- **速い層（既定・毎回）**: ホスト側 Node + Vitest のみ。Docker を触らない
- **遅い層（CI / 夜間）**: `make` 経由で実コンパイラを叩く差分テスト。
  QEMU で 1 コンパイル数秒なので、**開発ループには絶対に入れない**

`fixtures/` には配布物同梱の `pmind/sample/`（6 本）`sampleF/`（9 本）`file/`（46 本）を
UTF-8 へ変換して取り込む。**Mind 本体は再配布しない**（`vendor/*.tgz` は `.gitignore` 済み）ため、
`fixtures/` に入れるものは自作サンプル中心にし、同梱ソースは `make` でコンテナから取り出す形にする
（ライセンスは `README-mind8-linux.txt` に従う）。

---

## 8. リスクと未確定事項

### 解消済み ✅

| 旧リスク | 結果 |
|---|---|
| Mind 処理系が手元で動かない | **解消**。`linux/amd64` Docker + Mind 8 for Linux で疎通確認済み（`docs/COMPILER-BACKEND.md`） |
| 標準単語辞書が入手できない（`.sym` はバイナリ） | **解消**。`pmind/file/*.src` 46 本が同梱されており、辞書は自動生成できる |

### 現在のリスク

| リスク | 影響 | 対策 |
|--------|------|------|
| 正規化ルールの解釈が公式実装とズレる | 定義ジャンプ・補完が静かに外れる | **差分テストが実行可能になったので M0 で総当たり検証する**（最優先） |
| **検証は Mind 8、仕様の基準は Mind 9** | 9 で追加された単語・GUI 編が未検証 | 言語のコアは共通なので M0〜M4 は影響なし。辞書の差分は Windows 環境入手時に取り込む |
| **QEMU で 32bit x86 をエミュレート → 遅い** | 開発ループが詰まる／診断の応答が鈍い | LS 本体はホスト側ネイティブ。コンパイラ連携は保存時のみ。常駐コンテナ + `docker exec` も検討 |
| ソースの文字コード（SJIS / EUC-JP） | 文字化け・位置ズレ | UTF-8 前提で内部処理、境界だけ変換（`mindc` で実装済み）。オフセット変換にテストを書く |
| `（ ）` がコメントと括弧の両義 | 誤ハイライト・誤解析 | 「両端に空白」を厳密に判定。判定関数を単体テスト |
| 日本語識別子の補完フィルタが効かない | 補完が実質使えない | `filterText`/`sortText` を自前制御。必要なら読み仮名も辞書に持つ |
| `.src` が他言語（C/アセンブラ等）と拡張子衝突 | 誤って Mind と判定 | 設定で有効なグロブを指定できるようにする |
| リンク先ライブラリで可視な単語が変わる | 未定義単語の誤検出 | `mind.library` 設定 + `.his` からの自動判定 |
| `kernel/` の make が将来の gcc で通らなくなる | イメージがビルドできない | bookworm (gcc 12) を固定済み。最悪 `SKIP_KERNEL_MAKE=1` で同梱バイナリを使う |

### 残っている確認事項

- `.inf` の具体的なフォーマット（行番号・桁・メッセージの並び）
  → **`make` で意図的に文法エラーのある `.src` をコンパイルすれば今すぐ実物が得られる**。
  Mind 8 Linux 版に `lesson/bunpouerr.src` 相当があるかは `make inspect` で確認
- 副ソースの「読み出し」構文（複数ファイル構成）の正確な書式
- Mind 9 で追加された単語と `guilib` の語彙（Windows 環境が必要）

---

## 9. 見積り

| マイルストーン | 目安 |
|---------------|------|
| ~~M-1 Docker 環境~~ | **完了** ✅ |
| ~~M0 正規化~~ | **完了** ✅ |
| ~~M1 ハイライト~~ | **完了** ✅ |
| M0 正規化 PoC | 1〜2 日（+ 差分テストドライバ 1 日） |
| M1 ハイライト | 2〜3 日 |
| M2 パーサ/シンボル | 3〜5 日 |
| M3 定義ジャンプ/参照 | 2〜3 日 |
| M4 補完/ホバー | 3〜4 日 |
| **MVP 小計** | **11〜17 日** |
| M5 診断 | 4〜6 日 |
| M6 仕上げ | 3〜5 日 |
| **合計** | **18〜28 日** |

---

## 10. 次の一手

Docker 環境が動いたので、**「実物から取れるものを先に取る」**のが効率的。

1. **`make inspect` の出力を保存**し、`.inf` の実物を得る
   （文法エラー入りの `.src` を 1 本書いてコンパイルする）→ M5 のパーサ仕様が確定する
2. **標準ライブラリソースを取り出す**
   `npm run gen:stdlib   # 配布物の tgz から直接読む。Docker は不要`
   → M4 の辞書生成と M2 の `fixtures/` が同時に手に入る
3. `MindLS/` に npm workspaces のスケルトンを作る
4. `mind-core/src/normalizer.ts` + マニュアル由来のゴールデンテスト（M0）
5. `tools/diff-normalizer.ts` で実コンパイラとの差分テストを回す（M0 の完了条件）
6. M0 グリーン後に M1 のハイライトへ

---

## 参考資料

### リポジトリ内

- **`docs/COMPILER-BACKEND.md`** — 実 Mind コンパイラとの結合（構築済み・本書 2.7 / M5 の実装根拠）

### 公式

- [Mind9 プログラミングマニュアル（基礎編）](https://www.scripts-lab.co.jp/mind/ver9/doc/indexBase.html)
  - [2 プログラム表記の基本](https://www.scripts-lab.co.jp/mind/ver9/doc/02-Program-Hyoki.html) — 分かち書き・送り仮名・リテラル・コメント・定義
  - [3 制御構文](https://www.scripts-lab.co.jp/mind/ver9/doc/03-Control-syntax.html) — 制御構文キーワード全般
  - [4 データ型と変数](https://www.scripts-lab.co.jp/mind/ver9/doc/04-Data-and_variable.html) — 変数宣言・局所変数・スタック仕様
- [Mind9 オペレーションマニュアル](https://www.scripts-lab.co.jp/mind/ver9/doc/indexOperation.html) — コンパイラの起動方法・エラー出力
- [日本語プログラミング言語 Mind](https://www.scripts-lab.co.jp/mind/whatsmind.html)
- [Mind ダウンロードページ](https://www.scripts-lab.co.jp/mind/download/download.html) — Linux 版は Version 8 が最新
- [Mind8 オペレーションマニュアル 1b. インストール（Linux）](https://www.scripts-lab.co.jp/mind/ver8/doc/operation-1b-Install-linux.html)
- [Mind8 オペレーションマニュアル 2. ディレクトリ構成](https://www.scripts-lab.co.jp/mind/ver8/doc/operation-2-Directory.html)

### 先行事例

- 先行事例: [Mind をシンタックスハイライト tmLanguage](https://qiita.com/mylifewithviolin/items/5761f0f29255b121eee9)
- 先行事例: [Mind のプログラミングの基本のキ](https://qiita.com/Mind-Club_2017/items/bf6008a61a61a8c53673)
