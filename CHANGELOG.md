# 変更履歴

このファイルは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) にならい、
バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従います。

## [未リリース]

## [0.0.2] - 2026-09-17

### 追加

- **実コンパイラ連携**（`mind.compiler.enabled`、既定は無効）。保存したときに
  Docker の中の本物の `mind` に通し、`.inf` を読んで行と桁つきの診断にする。
  常駐コンテナに `docker exec` する方式で、ワークスペースは読み取り専用で渡す
- **取り込みの索引**。`"x.src"を　コンパイル。` でつながるファイルの大域シンボルを
  一緒に見るようになった

### 変更

- **未定義単語の診断を既定で有効にした**（`mind.diagnostics.undefinedWords`）。
  語彙を把握しきれないと分かったときは自動的に黙る — 取り込み先が見つからない、
  `mind.library` が `file` 以外、取り込みと無縁で `メイン` も無い（ライブラリ断片）

### 修正

実コンパイラに当てて分かった、診断の取り違えを直した。

- `。` を書かないままファイルが終わるのをエラーにしていた。**実コンパイラは通す**（EOF が定義を閉じる）
- 文字列定数の上限を 158 文字としていた。正しくは **32767 文字（半角換算）**（マニュアル 02）
- `表示（ここはコメントのつもり）し` のように処理単語に密着した括弧を見逃していた。
  実コンパイラは「かっこ表記に誤りが有ります」とエラーにする
- `○○は　文字列定数配列` のように `。` が数行あとに来る配列宣言を、処理単語の定義と誤読していた
- `合計（単価　→　金額）し` のように括弧の壊れた行で、割れた破片まで
  「未定義の単語です」と重ねて報告していた。割れたことを 1 回言うだけにした

## [0.0.1]

Mind のソースを VS Code で書くための最初の版です。Mind の配布物も Docker も要りません。

### 言語の理解

- 送り仮名・中点・全角半角・大文字小文字を畳む**正規化エンジン**。
  `繰り返す` も `繰り返し` も `繰返し` も同じ単語として扱います
- 分かち書き（区切りは TAB・空白・カンマ・読点だけ）に従うレキサ。
  `※` 行コメント、両端に空白のある `（ ）` コメント、`「」『』"" “”` の文字列、
  `終り。` 以降の非コンパイル領域、数式表現 `［ … ］` を区別します
- 制御構文の対応（`ならば`↔`つぎに`、`ここから`↔`繰り返す`、`事例をとる`↔`事例終り`）を見るパーサ
- **局所処理単語**（`下位処理とは` … `本体とは`）、型紙とその要素、
  ユーザー定義型による局所宣言（`管理テーブルは　ファイル情報`）に対応

### エディタの機能

- 構文ハイライト（tmLanguage）と、シンボルで塗り分ける **Semantic Tokens**。
  自分で定義した単語と標準ライブラリの単語が別の色になります
- アウトライン（定義の下に局所変数と局所処理単語がぶら下がります）
- 定義ジャンプ・参照検索・ワークスペースシンボル
- 補完（局所変数 → このファイルの定義 → 制御構文 → スニペット → 標準単語の順）
- ホバー（種別・スタック仕様・属性・定義行の抜粋、標準単語なら出典ファイル）
- **リネーム**（`売り上げ計上し` を `記帳` にすると `記帳し` になります。
  出現ごとの送り仮名と助詞はそのまま残ります）
- 入力中の字下げ（`ならば` の次を深く、`つぎに` `繰り返す` で浅く）

### 診断

- 構文の対応ミス、定義の `。` 閉じ忘れ、`事例をとる` に `例外なら` が無い、
  局所変数宣言に `。` を付けている、文字列定数が 158 文字超
- 定義より前での参照（`仮定義` が要る）
- 否定形の送り仮名（`表示しない` は送り仮名が落ちて肯定形と同じ単語になります）
- 空白不足で成立していない `（ ）` コメント
- 未定義単語（**既定では無効**。`"x.src"を　コンパイル。` の取り込み先をまだ追えないため）

既定で出る診断は、公式サンプルと標準ライブラリ 61 本に対して指摘ゼロであることを
テストで守っています。

### 標準単語辞書

- Mind の配布物から生成した大域 1,226 語を同梱（標準ライブラリ `pmind/file/*.src` と
  カーネル組み込み単語表 `pmind/kernel/c_words*.wrd` の両方）
- 辞書に載らない予約語（`入れる` `クリア` `一つ増加`、型名、コンパイラ指示、初等関数）は
  マニュアル由来の表として持っています

### そのほか

- 文字コードを設定するコマンド `Mind: このワークスペースの文字コードと関連付けを設定する`
  （Linux 版は EUC-JP、Windows 版は Shift_JIS）
- `Mind: Language Server を再起動する`

[未リリース]: https://github.com/shin3tky/MindLS/compare/v0.0.2...HEAD
[0.0.2]: https://github.com/shin3tky/MindLS/compare/v0.0.1...v0.0.2
[0.0.1]: https://github.com/shin3tky/MindLS/releases/tag/v0.0.1
